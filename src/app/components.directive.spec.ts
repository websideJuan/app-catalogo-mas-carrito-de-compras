import { ComponentsDirective } from './components.directive';

describe('ComponentsDirective', () => {
  it('should create an instance', () => {
    const directive = new ComponentsDirective();
    expect(directive).toBeTruthy();
  });
});
